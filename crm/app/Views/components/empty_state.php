<div class="card" role="status">
    <div class="card-body text-center py-5">
        <i class="feather-inbox fs-1 text-muted" aria-hidden="true"></i>
        <h2 class="h5 mt-3"><?= esc($emptyTitle ?? 'Sin resultados') ?></h2>
        <p class="text-muted mb-0"><?= esc($emptyMessage ?? 'No hay información disponible para mostrar.') ?></p>
    </div>
</div>
