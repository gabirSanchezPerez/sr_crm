# Seeds

The authoritative DDL contains no lookup rows, and production catalog/profile values are existing business data. No production seed is run during baseline adoption.

Test-only sanitized seeds will be added with the modules that require them. Production reference-data seeds require an approved inventory from a populated snapshot and must be idempotent.
